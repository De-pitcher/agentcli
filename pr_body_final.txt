fix(phase9): Fix critical bugs + improve planner goal_criterion + add sync test

### Bug Fixes

**1. pop_last_message() now syncs with MemoryStore**
- `session.py`: `pop_last_message()` now calls `memory_store.delete_last_message()` to sync both in-memory history and persistent SQLite storage
- `store.py`: Added `delete_last_message(session_id: str) -> bool` method to remove the most recent message from a session

**2. MemoryStore.__del__ crash fix**
- `store.py`: `close()` now uses `getattr(self, '_conn', None)` to safely handle missing `_conn` during interpreter shutdown
- Fixes `AttributeError: 'MemoryStore' object has no attribute '_conn'` during interpreter shutdown

### Planner Improvements

**3. Meaningful goal_criterion for reflector verification**
- `planner.py`: Set meaningful goal_criterion for all task types:
  - code_analyzer: "done" (matches mock output and real tool responses)
  - file_ops: operation-specific (file content, file written, etc.)
  - shell_execution: command-specific (test passed, build successful, etc.)
  - default: context-aware (explanation, code created, fix applied, done)
- LLM planner system prompt: improved to request verifiable goal_criterion

### New Test

**4. Integration test for pop_last_message + MemoryStore sync**
- Added `test_session_pop_last_message_syncs_to_store` to verify pop_last_message() properly syncs in-memory history with persistent SQLite storage

### Quality Gates
- 247 tests pass
- 89% coverage (≥85% floor)
- ruff: clean
- mypy: clean